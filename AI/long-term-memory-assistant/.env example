# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=node-postgres-demo
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Qdrant Configuration
QDRANT_URL=http://127.0.0.1:6333
QDRANT_API_KEY= # Optional: Add if your Qdrant instance requires authentication
QDRANT_COLLECTION_NAME=chat_history

# Gemini API Configuration
GEMINI_API_KEY=YOUR_GEMINI_API_KEY
GEMINI_EMBEDDING_MODEL=models/embedding-001 # Or your preferred Gemini embedding model
GEMINI_GENERATION_MODEL=models/gemini-1.5-pro-latest # Or your preferred Gemini generation model

# Server Configuration
PORT=3001
